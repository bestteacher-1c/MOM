#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	Если Параметры.Свойство("СтатьяРасходов") Тогда
		СтатьяРасходов = Параметры.СтатьяРасходов;
	КонецЕсли;
	
	Если НЕ ПолучитьФункциональнуюОпцию("ИспользуетсяЕНВД") Тогда
		Элементы.СчетСписанияЕНВД.Видимость = Ложь;
		Элементы.СчетСписанияОСНО.Заголовок = НСтр("ru = 'Счет списания'");
	КонецЕсли;
	
	ОтборыСписковКлиентСервер.ИзменитьЭлементОтбораСписка(
		Список, "СтатьяРасходов", СтатьяРасходов, ЗначениеЗаполнено(СтатьяРасходов));
	
	СобытияФорм.ПриСозданииНаСервере(ЭтаФорма, Отказ, СтандартнаяОбработка);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
Процедура СтатьяРасходовПриИзменении(Элемент)
	
	ОтборыСписковКлиентСервер.ИзменитьЭлементОтбораСписка(
		Список, "СтатьяРасходов", СтатьяРасходов, ЗначениеЗаполнено(СтатьяРасходов));
	
КонецПроцедуры

#КонецОбласти


